# SaltyOS Libraries Build
# SPDX-License-Identifier: GPL-2.0-only

# trona: system call library (substrate + posix + loader → libtrona.so, ldtrona-elf.so)
subdir('trona')

# basalt: C/C++ standard library (basaltc → libc.so, optional libc++.so)
subdir('basalt')
