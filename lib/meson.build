# SaltyOS Libraries Build
# SPDX-License-Identifier: GPL-2.0-only

# Libraries are built when userland is enabled
# For now, just define the structure

# libsalty - System call wrappers and IPC helpers
# libc - Minimal C library for userland

message('Libraries build configured (stubs only)')
