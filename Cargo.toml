[workspace]
resolver = "2"
members = [
    "abi/ska",
    "abi/ssabi",
    "abi/ssip",
    "kernel",
    "bootloader/uefi",
    "bootloader/common",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
