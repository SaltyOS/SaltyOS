[Service]
Name=dnssrv
Binary=/bin/dnssrv
Type=notify
Restart=always

[Dependencies]
After=netsrv namesrv mmsrv saltyfs vfs

[Capabilities]
# System roles via trona::caps::*, service-local via generated svc_caps::*.
Require=namesrv
Require=mmsrv
Require=netsrv:netsrv_ep
